# Sonarr-Radarr-Log-Scan

These scripts will allow you to scan your log files for unable to parse media info errors, and output a list that you can use to remove those files. Path mappings at the top are so it can translate your path if you are not running your arr stack on the same box as your library.

The nuke script will take the output from the log, and delete all of those files. 
